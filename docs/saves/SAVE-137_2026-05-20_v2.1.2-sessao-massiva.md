# SAVE-137 — 2026-05-20 — BOLD v2.1.2 (sessão massiva)

Versão final do dia: **v2.1.2**. Foram **9 releases** publicadas em uma sessão (v1.0.0 → v2.1.2).

## Resumo executivo

| Bloco | Status |
|---|---|
| Backoffice admin completo (Dashboard, Módulos, Aulas, Alunos, Comentários) | ✅ funcionando |
| 3 layouts de login com switcher live (Centro / Split / Vídeo BG) | ✅ Kaio precisa escolher o definitivo |
| VideoBackground próprio em motion graphics (SVG/CSS, sem dependência de vídeo real) | ✅ pronto até vídeo cinematográfico real chegar |
| HIBP no cadastro (k-anonymity, sem dependência do Pro Plan Supabase) | ✅ funcionando |
| hCaptcha real ativado | ✅ sitekey `0cf46e6c-8d98-4280-84d2-14c82f5e2d9c` |
| Revisão ortográfica completa pt-BR + sem travessões | ✅ todas as páginas |
| Scrollbar e cursor BOLD amarelos | ✅ |
| Splash 5s em dois momentos marco (institucional + ao logar) | ✅ |

## Schema final do Supabase BOLD

```
public/
├── profiles  (14 cols: full_name, avatar_url, phone, whatsapp NOT NULL,
│              cep, rua, numero, complemento, bairro, cidade, estado, pais, role)
├── modules
├── lessons
├── lesson_progress  (PK user_id+lesson_id)
├── comments         (com parent_id pra threads)
└── purchases        (gateway, transaction_id UNIQUE, raw_payload jsonb)

internal/                ← schema escondido do Data API
├── is_admin()
└── has_active_purchase(uuid)

storage/
└── avatars  (public read, write `name LIKE uid/%`)
```

0 advisors meus de segurança. Só sobra 1 do painel (Leaked Password Protection é paywall Pro, HIBP frontend já cobre).

## Rotas (17 telas)

**Públicas:** `/`, `/login`, `/cadastro`, `/recuperar-senha`, `/redefinir-senha`, `/checkout`, `/sucesso`
**Aluno:** `/dashboard`, `/modulo/:id`, `/aula/:id`, `/perfil`, `/certificado`
**Admin:** `/admin`, `/admin/modulos`, `/admin/modulos/:id`, `/admin/aulas/:id`, `/admin/alunos`, `/admin/comentarios`
**404:** `*`

## Decisões registradas

- Versionamento X.Y.Z: X = layout/estrutura, Y = CSS/JS, Z = correção
- Splash 5s em 2 marcos (institucional + ao logar no app)
- Cursor nativo do SO em amarelo BOLD (não usar cursor 3D customizado)
- Nunca usar travessões (em-dash) em código nem comunicação
- Sempre pt-BR completo com acentos
- Path do bucket avatars: `${user.id}/avatar-${Date.now()}.${ext}`
- Helpers SECURITY DEFINER em schema `internal/` pra não virarem RPC endpoints

## Pendências críticas (Kaio)

1. Rotacionar credenciais expostas no chat (service_role, secret_key, db pwd, vercel token, hCaptcha secret)
2. Converter repo pra private no GitHub
3. Escolher layout definitivo do login (testar o switcher)
4. Decidir gateway de pagamento (Kiwify recomendado)
5. Popular curso com módulos e aulas em `/admin`

## Commits da sessão (14)

```
d23b476 chore: initial setup com Vite+React19+TS+Tailwind v4+Supabase
3e8caa8 feat(db): schema inicial — 6 tabelas + RLS + helpers
d43c863 feat(app): área do aluno completa
1f30295 fix(vercel): SPA fallback com negative lookahead
1045592 fix(vercel): rewrite preservando assets
5a747d2 feat: v1.0.0 — perfil reestruturado + captcha + bg login
0867125 fix(db): v1.0.1 — permission denied on has_active_purchase
ad4579c feat: v1.1.0 — HIBP + scrollbar + cursor 3D
c2cc9cd fix(cursor): v1.1.1 — cursor nativo amarelo
0a3d8b1 feat: v2.0.0 — backoffice admin completo
97afd66 fix(storage): v2.0.1 — RLS avatars name LIKE
a7f0821 feat: v2.1.0 — 3 layouts de login com switcher
3bd9fc1 fix: v2.1.1 — VideoBackground próprio + revisão ortográfica pt-BR
6a1fc50 chore: bump v2.1.2 (housekeeping)
```

## Fixes paralelos no AgroNortão (não neste repo, mas registrados aqui)

1. Bucket `fotos-cupons` permitia upload só pra anon — quebrou pra admin logado. Fix: 4 policies amplas sem restrição de role.
2. Tabela `empresas_meta` não tinha colunas `email` e `observacoes` que o app esperava. Fix: ALTER TABLE adicionando as 2 colunas.
